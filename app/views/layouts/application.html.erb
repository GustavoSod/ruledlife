<!DOCTYPE html>
<html class="h-full">
<head>
  <title>RuledLife</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
</head>

<body class="bg-zinc-900 flex flex-col h-full">
  <div id="header" class="flex items-center justify-around bg-zinc-950 p-8">
    <div>
      <%= link_to "RuledLife", root_path, class: "text-4xl text-white font-semibold" %>
    </div>
    <div class="sm:flex hidden">
      <ul class="flex items-center text-white text-base">
        <% if user_signed_in? %>
          <%= link_to "Tarefas", tasks_path, class: "p-2 rounded-xl cursor-pointer ml-4" %>
          <%= link_to "Dietas", dishes_path, class: "p-2 rounded-xl cursor-pointer ml-4" %>
          <%= link_to "Treinos", treinos_path, class: "p-2 rounded-xl cursor-pointer ml-4" %>
          <%= button_to "Sair", destroy_user_session_path, method: :delete, class: "bg-red-700 p-2 rounded-xl cursor-pointer ml-4" %>
        <% else %>
          <%= link_to "Entrar", new_user_session_path %>
          <%= link_to "Cadastre-se", new_user_registration_path, class: "ml-4" %>
          <button onclick="document.getElementById('professorModal').showModal()" class="bg-red-700 p-2 rounded-xl cursor-pointer ml-4">Ser Professor</button>
        <% end %>
      </ul>
    </div>
  </div>

  <div id="content" class="flex-1">
    <%= yield %>
  </div>

  <div id="footer" class="w-full sm:h-[300px] sm:p-24 bg-zinc-950 flex sm:flex-row flex-col items-center justify-around">
    <div>
      <h3 class="sm:mt-0 mt-28 text-4xl text-white font-semibold">RuledLife</h3>
      <p class="text-gray-300 text-xs mt-4">Av Tal lala 123 </p>
      <p class="text-gray-300 text-xs mt-4"> XX. XXX. XXX/0001-XX </p>
    </div>
    <div>
      <h3 class="sm:mt-0 mt-28 text-2xl text-white font-semibold">Redes Sociais</h3>
      <p class="text-gray-300 text-xs mt-4">Instagram / Facebook </p>
      <p class="text-gray-300 text-xs mt-4">@todos os direitos reservados</p>
    </div>
    <div class="sm:mb-0 mb-16">
      <h3 class="sm:mt-0 mt-28 text-2xl text-white font-semibold">Contato</h3>
      <p class="text-gray-300 text-xs mt-4">0800 333 333</p>
      <p class="text-gray-300 text-xs mt-4">contato@ruledlife.com</p>
    </div>
  </div>

  <!-- Modal -->
  <dialog id="professorModal" class="bg-zinc-900 rounded-lg p-14 max-w-md mx-auto">
    <h2 class="text-4xl text-white font-bold mb-4">Em <span class="text-red-700">Breve</span></h2>
    <p class="mb-4 text-gray-300">Função de professor vai ser lançada em breve, enquanto não for lançada aproveite as outras funcionalidades do nosso site!</p>
    <button type="button" onclick="document.getElementById('professorModal').close()" class="mt-4 bg-red-700 text-white px-4 py-2 rounded">Fechar</button>
  </dialog>

  <%= javascript_include_tag "application", "data-turbo-track": "reload" %>
</body>
</html>
